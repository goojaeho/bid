# 자비스 0.5 검증 결과 — 2026-09-17

## 적용 범위
- Windows 자비스 ↔ One AI Gen 소유자 전용 API ↔ 기존 Google 연결과 저장소.
- 기록 검색은 **mail_items 메일과 meetings 회의록만**. 지니/즐겨찾기/장소/저장 검색/할 일은 이 검색에서 제외. 할 일 브리핑은 종전 기능 유지.
- Mac 모델은 EXAONE 4.0 1.2B MLX 4bit 평가용. F1 여성 음성 유지.

## 확인한 결과
- 실제 Google 권한 상태: Gmail 읽기 성공, 캘린더 읽기/등록/수정 가능. gmail.modify는 아직 미동의.
- 실제 기본 캘린더: 7일 일정 14개 조회.
- 실제 임시 일정: 승인 처리 경로로 등록 → 재조회 → 동일 요청 재시도 중복 방지 → 겹침 감지 → ETag 조건부 수정 → 삭제 모두 성공. 시험 일정은 자동 삭제했다.
- 실제 사이트 저장 메일 717개를 페이지별 모두 가져와 검색. 회의록은 현재 0개. 미조회 소스/페이지가 있으면 오류를 표시하고 완전한 검색이라고 안내하지 않는다.
- 앱 메일 목록 100개 및 실제 원본 메일 본문 읽기 성공. 1분 간격으로 최근 7일 받은편지함을 페이지별 수집. 최초 과거 메일은 소리로 쏟아내지 않는다. 광고 분류는 기존 사이트 분류 또는 Gmail Promotions 라벨을 사용한다.
- 합성 알림으로 안내 중지 중 침묵, 재개 후 요약 전달 검증. 실제 새 메일을 발송하여 시험하지 않았으며 기존 메일을 휴지통에 옮기지 않았다.
- 앱에서 수동 캘린더 제안 UI, 승인 전 미저장 확인.
- 실제 EXAONE: ‘내일 오후 3시에 30분 동안 테스트 회의 잡아줘’ → 2026-09-18 15:00 / 테스트 회의 / 30분 제안. 첫 성공 3.374초, 최종 실행 파일에서 3.274초. 제안 취소까지 시험했으며 이 문장의 일정을 실제 등록하지 않았다.
- Python 88개, 데스크톱 단위시험 12개 통과. 실제 저장/수정은 별도 임시 일정 검증으로 확인.

## 동작 및 제한
- 메일을 읽고 일정 후보가 있으면 제안한다. 사용자가 해당 제안을 승인해야 캘린더에 반영한다. 휴지통 이동도 해당 메일의 확인 버튼을 눌러야 한다. 영구 삭제 기능은 없다.
- 휴지통 이동 실행은 현재 Google 권한 때문에 막힌 상태다. 앱 ‘메일 · 캘린더 · 기록 검색’ → ‘Google 연결·권한 갱신’에서 기존 연결 계정으로 동의해야 한다. 기능 코드와 소유자 제한은 시험했지만 실제 휴지통 이동 성공으로 보고하지 않는다.
- 충돌 검사는 연결 계정의 **기본 캘린더** 기준. 모든 공유 캘린더/참석자 초대/반복 일정 편집은 범위 밖이다. 참석자가 있는 기존 일정 수정은 Google 캘린더에서 처리한다.
- 기록 검색은 사이트에 저장된 텍스트 전체를 키워드로 검색한다. 저장 메일에 원본 전체 본문이 없으면 제목/발신자/요약/스니펫/일정 후보만 검색된다. 원본 메일 읽기는 별도 Gmail 조회로 제공하며 첨부파일 검색은 하지 않는다. 회의록은 저장된 전사와 minutes 포함.
- 모델에는 관련 발췌만 전달한다. 조회 오류/발췌 제한은 구분하고 원문 지시를 실행하지 않는다. EXAONE의 복잡한 문맥/요약은 확인 질문으로 돌아갈 수 있다.
- EXAONE 공식 NC1.2 라이선스의 평가/시험 목적 적용이다. 일반 업무 운영 사용이 무제한 허용된다는 뜻이 아니다. https://huggingface.co/LGAI-EXAONE/EXAONE-4.0-1.2B/raw/main/LICENSE
- Mac 재부팅 후 서버 자동 실행은 기존처럼 미완료. 현재 서버 8765/8766 실행 중.

## 재현
- `.venv/Scripts/python.exe -m unittest discover -s tests -q`
- `cd desktop; npm test`
- `node services-smoke.cjs`: 실제 읽기/미저장 제안, 실제 메일 삭제나 실제 사용자 일정 등록 없음.
- `node notifications-smoke.cjs`: 합성 큐 중지/재개.
- `electron service-probe.cjs status`: 권한 bool만 출력, 비밀 토큰 출력 없음.
- `electron service-probe.cjs calendar.verify`: 임시 시험 일정 생성/수정/삭제를 수반하므로 테스트 의도 있을 때만 실행.
