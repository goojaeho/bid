# 입찰정보 검색 프로그램 개발 계획

> 한국 공공 입찰(나라장터 등) 정보를 수집·검색·알림할 수 있는 프로그램의 개발 계획서.
> 참고 프로젝트: [melaka-ybbae/bid-crawler](https://github.com/melaka-ybbae/bid-crawler)
> (나라장터 API + 사립대 게시판 크롤링, Python 기반 Windows 데스크톱 앱)

---

## 1. 핵심 결론 (요약)

| 질문 | 결론 |
|---|---|
| API를 쓸까, 크롤링을 할까? | **공공데이터포털 나라장터 OpenAPI 사용 (완전 무료)** — 크롤링은 API가 없는 소스에만 보조적으로 |
| 공짜로 가능한가? | **가능.** API 키 발급·호출 모두 무료. 서버 없이 로컬 실행하면 비용 0원 |
| 검색 조건(항목·기간 등)? | API가 업무구분(물품/용역/공사/외자), 기간, 공고명 검색을 파라미터로 지원 |
| 웹 vs 설치형? | **웹 추천.** 단, 처음엔 로컬에서 띄우는 웹앱(설치 부담 없음)으로 시작 → 필요 시 서버 배포 |

---

## 2. 데이터 소스 전략

### 2.1 1차 소스: 공공데이터포털 OpenAPI (무료)

**조달청_나라장터 입찰공고정보서비스** ([data.go.kr/data/15129394](https://www.data.go.kr/data/15129394/openapi.do))

- 공공데이터포털(data.go.kr) 회원가입 → 활용신청 → API 키 발급 (무료, 자동승인)
- 제공 데이터: 물품·용역·공사·외자 **입찰공고 목록/상세**, 기초금액, 면허제한, 참가가능지역, 공고 변경이력
- 주요 검색 파라미터:
  - `inqryBgnDt` / `inqryEndDt` — 조회 기간 (공고일 기준)
  - `bidNtceNm` — 공고명 키워드
  - 업무구분별 오퍼레이션 분리 (물품/용역/공사/외자 → 각각 다른 엔드포인트 호출 필요)
- 개발계정 기준 일일 트래픽 제한 있음(통상 1만 건/일) — 개인·소규모 용도로는 충분

**추가로 붙일 수 있는 무료 API** (2단계 이후 확장):
- 조달청_나라장터 계약과정통합공개서비스 — 개찰/낙찰 결과 추적용
- 온비드(캠코) API — 공매·자산처분
- 국방전자조달(D2B), 각 공기업 전자조달 — 필요 시

### 2.2 2차 소스: 크롤링 (API가 없는 곳만)

- 참고 프로젝트처럼 대학교·기관 홈페이지 게시판 등 API 미제공 소스는 크롤링으로 보완
- 원칙: **API 있으면 무조건 API** (안정성·약관·유지보수 측면에서 크롤링보다 우위)
- 크롤링 시 robots.txt 및 사이트 이용약관 준수, 요청 간격 제한(rate limit) 적용

---

## 3. 배포 형태: 웹 vs 설치형

| 기준 | 웹 | 설치형(참고 프로젝트 방식) |
|---|---|---|
| 접근성 | 브라우저·모바일 어디서나 | 설치한 PC에서만 |
| 배포/업데이트 | 서버만 갱신하면 끝 | exe 재빌드·재배포 필요 |
| 검색 UI | 테이블·필터 구현 자유로움 | GUI 프레임워크 제약 |
| 비용 | 서버 배포 시 소액 발생 가능 | 0원 |
| 알림 | 이메일/텔레그램/웹푸시 | Windows 토스트 |

**추천: 단계적 웹 접근**
1. **1단계 — 로컬 웹앱**: `python` 하나로 로컬에서 실행 → 브라우저로 접속. 서버 비용 0원, 설치형의 장점(공짜)과 웹의 장점(UI 자유도)을 동시에 확보
2. **2단계 — 필요해지면 배포**: 무료~저가 티어(Fly.io, Railway, 홈서버, Oracle 무료 VM 등)로 올려 모바일에서도 접근

---

## 4. 기술 스택 (제안)

- **언어**: Python 3.11+ (참고 프로젝트와 동일 계열이라 코드 참고 용이)
- **백엔드**: FastAPI — API 수집 스케줄러 + 검색 REST API 제공
- **DB**: SQLite — 파일 하나, 설치 불필요, FTS5로 한글 키워드 전문검색 가능. 규모 커지면 PostgreSQL로 이전
- **수집 스케줄러**: APScheduler (앱 내장) — 주기적으로 나라장터 API 폴링
- **프론트엔드**: 처음엔 서버사이드 템플릿(Jinja2) + HTMX로 단순하게. 화면이 복잡해지면 React/Vue 도입 검토
- **알림(선택)**: 텔레그램 봇(무료·구현 간단) 또는 이메일

## 5. 데이터 모델 (초안)

```
bid_notice (입찰공고)
├─ id                  # 공고번호-차수 (고유키, 중복 수집 방지)
├─ category            # 업무구분: 물품/용역/공사/외자
├─ title               # 공고명
├─ org_demand          # 수요기관
├─ org_notice          # 공고기관
├─ notice_date         # 공고일시
├─ close_date          # 입찰마감일시
├─ open_date           # 개찰일시
├─ base_amount         # 기초금액
├─ region              # 참가가능지역
├─ url                 # 나라장터 상세 URL
├─ source              # 수집 출처 (g2b_api / 크롤링 사이트명)
└─ collected_at        # 수집 시각

keyword (관심 키워드)      # 사용자 등록 키워드 → 매칭 시 알림
search_log / bookmark    # (선택) 검색 이력, 관심 공고 저장
```

## 6. 핵심 기능 명세

### MVP (1차 목표)
- [ ] 나라장터 API 수집기: 물품/용역/공사/외자 4개 오퍼레이션 주기 수집, 공고번호 기준 중복 제거
- [ ] 검색 화면:
  - 키워드 검색 (공고명)
  - 기간 필터 (공고일 / 마감일 기준)
  - 업무구분 필터 (물품/용역/공사/외자)
  - 기관·지역·금액 범위 필터
  - 정렬 (공고일순 / 마감임박순 / 금액순)
- [ ] 공고 상세 보기 + 나라장터 원문 링크
- [ ] 수동 새로고침 버튼 (즉시 수집)

### 2차 확장
- [ ] 관심 키워드 등록 → 신규 매칭 공고 텔레그램/이메일 알림
- [ ] 관심 공고 북마크 + 마감일 D-day 표시
- [ ] 개찰/낙찰 결과 추적 (계약과정통합공개서비스 API 연동)
- [ ] 대학·기관 게시판 크롤러 추가 (참고 프로젝트의 크롤러 로직 이식)
- [ ] 엑셀(xlsx) 내보내기

## 7. 개발 단계 및 일정 (개인 개발 기준)

| 단계 | 내용 | 예상 기간 |
|---|---|---|
| 0. 준비 | 공공데이터포털 가입, API 활용신청, 응답 스키마 확인 (curl/Postman으로 실제 호출 테스트) | 0.5일 |
| 1. 수집기 | API 클라이언트 + SQLite 저장 + 중복제거 + 스케줄러 | 2~3일 |
| 2. 검색 API | FastAPI 검색 엔드포인트 (키워드·기간·구분·정렬·페이징) | 1~2일 |
| 3. 웹 UI | 검색 폼 + 결과 테이블 + 상세 페이지 | 2~3일 |
| 4. 알림 | 키워드 매칭 + 텔레그램 알림 | 1~2일 |
| 5. 확장 | 크롤러 추가, 낙찰 결과, 배포 | 이후 순차 |

**MVP까지 약 1~2주** (파트타임 기준).

## 8. 리스크 및 유의사항

- **API 키 관리**: 키는 `.env` 파일에 두고 git에 커밋하지 않기 (`.gitignore` 등록)
- **트래픽 제한**: 개발계정 일일 한도 내에서 수집 주기 설계 (예: 30분~1시간 간격이면 충분)
- **API 스펙 변경**: 조달청이 서비스 개편 시 엔드포인트가 바뀔 수 있음 → API 클라이언트를 한 모듈로 격리해 교체 용이하게
- **날짜 파라미터 형식**: `YYYYMMDDHHMM` 형식 요구 — 파싱 유틸 필수
- **업무구분별 엔드포인트 분리**: 물품/용역/공사/외자를 각각 호출해야 전체 커버 가능
- **크롤링 법적 이슈**: 공공 API 우선, 크롤링은 공개 게시판에 한해 저부하로

## 9. 다음 액션

1. [공공데이터포털](https://www.data.go.kr/data/15129394/openapi.do)에서 "나라장터 입찰공고정보서비스" 활용신청 → API 키 발급
2. 발급된 키로 샘플 호출해 실제 응답 필드 확인
3. 위 4번 스택으로 프로젝트 뼈대 생성 → 1단계(수집기)부터 착수
