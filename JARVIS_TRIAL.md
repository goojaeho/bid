# 자비스 첫 시험

현재 구현: 소유자 전용 `/jarvis` 화면에서 마이크 버튼 또는 글로 명령하면 기존 Supabase 할 일 저장소에 등록하고, 요청 시 미완료 할 일을 브리핑한다. AI API를 호출하지 않는 제한된 명령 처리이며 브라우저 음성 인식 서비스와 음성 합성을 사용한다. 인식 서비스의 제공 여부는 브라우저 및 네트워크에 따라 달라진다.

## 실제 계정 시험

기존 배포와 동일한 Google 로그인, OWNER_EMAIL(또는 첫 ADMIN_EMAILS), Supabase 설정이 필요하다. 테스트 배포에 로그인 리디렉션 URI를 설정한다. 실제 비밀키는 문서나 저장소에 넣지 않는다.

1. 소유자로 로그인하고 `/jarvis`에 접속한다.
2. 마이크 권한을 허용한 뒤 “자비스, 내일 보고서 작성 할 일 등록해줘”라고 말한다.
3. `/todo`에서 제목과 마감일을 확인한다.
4. “브리핑해봐”라고 말해 기존 할 일이 반영됐는지 확인한다.
5. 음성 중지 버튼으로 읽기를 중단한다.

이번 구현은 시간 지정 약속을 거절한다. 충돌 확인, Google Calendar 동기화, Gmail 분석, Codex 구독 연결, 상시 호출, Windows 트레이 실행, Android 통화 수집은 후속 단계다. 창을 닫으면 음성 입력도 종료된다. 배포 및 실제 계정/마이크 테스트 전에는 실사용 검증 완료로 간주하지 않는다.

## 확정 요구사항

- 본인 계정 전용. Mac mini M1을 계속 켜서 처리, Windows에서 자비스 호출과 음성 안내.
- 직접 지시는 실행하되 충돌은 먼저 조정. 전화/메일에서 발견한 일정 및 준비 작업은 승인 후 등록.
- 브리핑은 요청할 때만. 약속 전 자동 알림 없음.
- Windows 창 종료 후에도 최종 앱은 상주. 회의 중 중지/재개 명령, 재개 시 밀린 안내 요약 및 개별 읽기.
- 광고 메일은 브리핑에서 모아서 제안하고 승인 후 휴지통 이동.
- 모든 통화 분석 결과는 폰에서 안내하고 등록/수정/거절 버튼 표시. 무음/진동이면 소리 없이 알림과 버튼만 표시.
- 기존 사이트와 Google Calendar 연동. 기존 구독 활용을 우선하며 추가 AI 과금은 자동 활성화하지 않음.
