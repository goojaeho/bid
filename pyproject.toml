[project]
name = "bid"
version = "0.1.0"
description = "나라장터 입찰공고 수집·검색"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[tool.vercel]
entrypoint = "api/index.py"
